<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nbufe.zfr.UniTakeout_server.mapper.DelegationMapper">
    <select id="selectById" resultType="Delegation">
        SELECT * FROM delegation WHERE id = #{id}
    </select>
    
    <select id="selectList" resultType="Delegation">
        SELECT * FROM delegation
        <where>
            <if test="type != null and type != ''">
                type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="count" resultType="Long">
        SELECT COUNT(*) FROM delegation
        <where>
            <if test="type != null and type != ''">
                type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO delegation (type, user_id, user_name, title, description, shop_id, shop_name, reward, status)
        VALUES (#{type}, #{userId}, #{userName}, #{title}, #{description}, #{shopId}, #{shopName}, #{reward}, #{status})
    </insert>
    
    <update id="update">
        UPDATE delegation
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="acceptedBy != null">accepted_by = #{acceptedBy}</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>

