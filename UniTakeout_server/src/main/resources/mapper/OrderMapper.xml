<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nbufe.zfr.UniTakeout_server.mapper.OrderMapper">
    <select id="selectById" resultType="Order">
        SELECT * FROM `order` WHERE id = #{id}
    </select>
    
    <select id="selectByOrderNo" resultType="Order">
        SELECT * FROM `order` WHERE order_no = #{orderNo}
    </select>
    
    <select id="selectByUserId" resultType="Order">
        SELECT * FROM `order`
        WHERE user_id = #{userId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="countByUserId" resultType="Long">
        SELECT COUNT(*) FROM `order`
        WHERE user_id = #{userId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (order_no, user_id, shop_id, shop_name, status, contact_name, contact_phone,
                           address, pay_method, goods_amount, delivery_fee, total_amount, remark)
        VALUES (#{orderNo}, #{userId}, #{shopId}, #{shopName}, #{status}, #{contactName}, #{contactPhone},
                #{address}, #{payMethod}, #{goodsAmount}, #{deliveryFee}, #{totalAmount}, #{remark})
    </insert>
    
    <update id="update">
        UPDATE `order`
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
            <if test="deliveryTime != null">delivery_time = #{deliveryTime}</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>

